{
  "name": "kayvee",
  "description": "a tiny key value database",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "scripts": {
    "start:dev": "nodemon ./dist",
    "watch:tsc": "rm -rf ./dist && tsc --watch -p ./tsconfig.json",
    "build:tsc": "tsc -p ./tsconfig.json",
    "test": "jest"
  },
  "author": "Raymond Tukpe <rtukpe@gmail.com>",
  "dependencies": {
    "typescript": "^4.3.2"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@types/node": "^15.12.1",
    "jest": "^27.0.3",
    "ts-jest": "^27.0.2",
    "ts-node": "^10.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
